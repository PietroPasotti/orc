"""orc.git — git operations subpackage.

Submodules:
- :mod:`orc.git.core` — worktree, branch, merge, and state-derivation helpers
- :mod:`orc.git.conflict` — conflict resolution via agent delegation
"""
